{
  "name": "learnmore-mobile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "test": "node --test tests/*.test.mjs",
    "ios:sync": "npm run build && cap sync ios",
    "ios:open": "cap open ios"
  },
  "dependencies": {
    "@capacitor/app": "^8.0.0",
    "@capacitor/browser": "^8.0.0",
    "@capacitor/core": "8.5.1",
    "@capacitor/ios": "8.5.1",
    "dompurify": "^3.3.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.1",
    "typescript": "^5.9.3",
    "vite": "^7.3.0"
  },
  "overrides": {
    "xcode": { "uuid": "^11.1.1" }
  }
}
